[package]
name = "kubera-sbe"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-sbe"
publish = false

[dependencies]
# Re-export from quantlaxmi-sbe (implementation moved there)
quantlaxmi-sbe = { path = "../quantlaxmi-sbe" }
