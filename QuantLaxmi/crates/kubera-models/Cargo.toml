[package]
name = "kubera-models"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-models"
publish = false

[dependencies]
# Re-export from quantlaxmi-models (implementation moved there)
quantlaxmi-models = { path = "../quantlaxmi-models" }
