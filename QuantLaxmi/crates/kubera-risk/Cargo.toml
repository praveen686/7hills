[package]
name = "kubera-risk"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-risk"
publish = false

[dependencies]
# Re-export from quantlaxmi-risk (implementation moved there)
quantlaxmi-risk = { path = "../quantlaxmi-risk" }
