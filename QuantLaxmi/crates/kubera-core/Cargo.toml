[package]
name = "kubera-core"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-core"
publish = false

[dependencies]
# Re-export from quantlaxmi-core (implementation moved there)
quantlaxmi-core = { path = "../quantlaxmi-core" }
