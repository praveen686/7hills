[package]
name = "kubera-data"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-data"
publish = false

[dependencies]
# Re-export from quantlaxmi-data (implementation moved there)
quantlaxmi-data = { path = "../quantlaxmi-data" }
