[package]
name = "kubera-options"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-options"
publish = false

[dependencies]
# Re-export from quantlaxmi-options (implementation moved there)
quantlaxmi-options = { path = "../quantlaxmi-options" }
