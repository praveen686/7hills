[package]
name = "kubera-executor"
version = "0.1.0"
edition = "2021"
description = "Legacy shim - re-exports quantlaxmi-executor"
publish = false

[dependencies]
# Re-export from quantlaxmi-executor (implementation moved there)
quantlaxmi-executor = { path = "../quantlaxmi-executor" }
