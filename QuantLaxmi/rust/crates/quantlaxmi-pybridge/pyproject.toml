[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "quantlaxmi-rs"
version = "0.1.0"
description = "PyO3 bridge for QuantLaxmi hot-path acceleration"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
